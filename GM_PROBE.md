# gm-probe —— 让网关对任意 MLX 模型都能自动加速

> **一句话**：一条命令完成「探测模型 → 匹配同族草稿 → 校验兼容性 → 推荐/实测配置 → 写回」，
> 把「换一个 MLX 模型」从手工四步变成一条命令。

---

## 先说清楚能达到什么、达不到什么

你的目标是**对所有 MLX 模型都有加速效果**。我必须诚实说明：

### ✅ 能做到的（有条件的普适）

**凡是有「同族小草稿」的模型，都能自动获得投机解码加速。**

| 目标模型 | 同族草稿 | 草稿成本比 c | 实测加速 |
|---|---|---|---|
| Llama-3.1-8B | Llama-3.2-1B | ~0.125 | **1.245–1.284×** |
| Llama-3.1-70B | Llama-3.2-1B | ~0.014 | 更大（未实测） |
| Qwen2.5-7B/14B/32B | Qwen2.5-0.5B | ~0.07 | 更大（未实测） |
| Gemma / Phi / Mistral 各族 | 各族小模型 | — | — |

**主流大模型族基本都有小兄弟**，所以这条覆盖面很广。

### ❌ 做不到的（数学上不可能）

**小模型无法通过投机解码加速。** 收益公式：

```
收益 = 接受长度 / (1 + c)，   c = 草稿时间 / 目标时间
```

要加速，必须 `c < 1`，即**草稿必须比目标模型小**。

- **Qwen2.5-0.5B**：没有比它更小的 Qwen。拿 1.5B 当草稿 → `c > 1` → **必然更慢**。
- **刚发布的新架构**：还没有对应的小模型 → 无草稿可用。

**这不是实现缺陷，是投机解码的数学前提。** `gm-probe` 遇到这类模型时会
**明确报告「不支持加速」并给出原因**，而不是硬配一个草稿让你以为有加速。

### 🟨 对这类模型仍然有效的两个杠杆

它们**与模型族完全无关**，任何 MLX 模型都能用 —— 但加速的是 **TTFT / 内存**，
不是 decode 吞吐：

| 杠杆 | 机制 | 效果 |
|---|---|---|
| `prefix_cache` | 多轮对话复用 KV 前缀（只用 `trim_prompt_cache`） | TTFT 大幅下降（实测 ctx=5646 时 22.3×） |
| `prefix_cache.kv_bits=8` | int8 KV 量化 | 内存 −1.53 GB（9.03 → 7.50 GB） |

---

## 用法

```bash
cd /path/to/proteus-studio

# 1. 探测（只读，不改任何文件）
./gm-probe /path/to/your/model

# 2. 探测 + 实测扫描 n_draft（较慢，会真实加载模型）
./gm-probe /path/to/your/model --sweep

# 3. 写回 models.json（自动备份，然后重启网关）
./gm-probe /path/to/model --write --name my-model --alias mm
launchctl kickstart -k gui/$(id -u)/com.tristan.gm.gateway

# 4. 手工指定草稿（跳过自动匹配）
./gm-probe /path/to/target --draft /path/to/draft

# 5. JSON 输出（供脚本消费）
./gm-probe /path/to/model --json
```

---

## 它做了哪些检查

| 步骤 | 检查内容 | 为什么必须 |
|---|---|---|
| 1 | **快照完整性** | HF cache 里可能有只下载了 `config.json` 的半成品，会让后续报出误导性的「词表不兼容」 |
| 2 | **模型指纹** | 读 `model_type` / `vocab_size` / `hidden_size` / `num_layers` |
| 3 | **同族草稿匹配** | 按 `model_type` 查内置候选表，只在**本地缓存**里找（不静默联网） |
| 4 | **tokenizer 兼容性** | vocab size + BOS 归一化后的探针编码（详见 `mlx_lm_backend._tokenizers_compatible`） |
| 5 | **规模关系** | 草稿的 `hidden_size` 必须 **<** 目标的 —— 否则 `c ≥ 1`，投机必然亏损 |
| 6 | **n_draft 扫描**（可选） | 实测各 nd 的 tok/s，取最优 |

---

## 已知缺陷与注意事项

### ⚠️ `--sweep` 在本机无法可靠区分 nd（已实测确认）

**第一版的 bug（已修）**：顺序执行 nd=1→N，把 nd=4 测成 8.69 tok/s
（nd=2 是 22.08），看起来像灾难性退化。交替复测的比值只有 **0.969** ——
纯热假象。已改为**轮转交错**（每轮所有 nd 各测一次）+ 报比值。

**但改了之后仍然不可靠。** 2026-09-16 跑了四次独立扫描，最优值分别是
**2 / 2 / 1 / 3** —— 四个不同答案。逐 nd 比值跨度：

| nd | 四次比值 | 跨度 |
|---|---|---|
| 1 | 0.926 / 1.000 / 0.920 | 0.080 |
| 2 | 0.958 / 1.000 / 0.810 / 0.976 | **0.190** |
| 3 | 1.000 / 0.953 / 0.835 | 0.165 |
| 4 | 0.914 / 0.932 / 1.000 | 0.086 |

**结论：在 96-token 短序列上，本机无法区分 nd=1..4。**

因此工具现在：
1. 用 **5% 噪声带**判定并列，报出**整个并列区间**而非单一"最优值"
2. 给出**可信度分级**（`LOW` / `MEDIUM`），并列时明确说"本机无法区分"
3. 并列时按 `accept_len` 择优 —— 它是**纯算法量、不受热态影响**

🟨 **用户应当这样解读**：若输出 `可信度: LOW`，说明 nd 在这个范围内
随便选都差不多，**不要为了那 5% 反复调参**。真正有意义的信号是
`accept_len`（每轮接受多少 token），它稳定且可复现。

**要拿到可信的 nd，需要**：更长的生成序列（≥200 token 而非 96）、
更多轮次、以及轮次间冷却到功耗恢复 —— 当前实现都没做。

### ⚠️ nd 的默认值不适用于所有模型

不加 `--sweep` 时，`nd=3` 是**沿用 Llama-3.1-8B + 3.2-1B 的实测值**，不是通解。
最优 nd 取决于 target/draft 的相对速度，**换模型必须重测**。

报告里会明确标注 `nd_is_default_not_measured: true`。

### ⚠️ 不静默联网

`gm-probe` 只在本地 HF cache 里找草稿。找不到会提示「需先下载」，不会自动
触发下载 —— 避免静默产生大流量网络副作用。

---

## 内置的同族草稿表

见 `gm/autotune.py` 的 `DRAFT_CANDIDATES`。当前收录：

| model_type | 候选草稿 |
|---|---|
| `llama` | Llama-3.2-1B / Llama-3.2-3B |
| `qwen2` | Qwen2.5-0.5B / Qwen2.5-1.5B |
| `qwen3` | Qwen3-0.6B / Qwen3-1.7B |
| `gemma2` | gemma-2-2b-it |
| `gemma3` | gemma-3-1b-it |
| `phi3` | Phi-3.5-mini |
| `mistral` | Mistral-7B-v0.3 |
| `granite` | granite-3.1-2b |

**未收录的架构**：在 `DRAFT_CANDIDATES` 里加一行即可，无需改其他代码。

---

## 验证记录（2026-09-16）

| 场景 | 结果 |
|---|---|
| Llama-3.1-8B（有同族草稿） | ✅ 正确识别并推荐 |
| Qwen2.5-0.5B（小模型，无更小草稿） | ✅ 诚实报告「不支持加速」+ 给出通用杠杆 |
| Qwen2.5-1.5B（半成品快照） | ✅ 正确识别为「目录不完整」，而非误报词表不兼容 |
| `--write` 对不支持的模型 | ✅ 拒绝写入，`models.json` 未被改动 |
| `--write` 对支持的模型（隔离目录） | ✅ 正确写入 + 生成备份 |
| 负对照：手工指定更大的草稿 | ✅ 被规模检查拦下 |

---

## 回滚

`--write` 每次都会备份：`models.json.bak.YYYYmmdd_HHMMSS`。
恢复：`cp models.json.bak.<戳> models.json && launchctl kickstart -k gui/$(id -u)/com.tristan.gm.gateway`
